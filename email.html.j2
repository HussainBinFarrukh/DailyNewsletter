<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>{{ subject }}</title>
  <style>
    body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,Helvetica,Arial,sans-serif;margin:0;background:#f7f7fb;color:#111}
    .wrap{max-width:720px;margin:0 auto;padding:24px}
    .card{background:#fff;border-radius:14px;padding:20px;margin:16px 0;border:1px solid #eee}
    h1{font-size:24px;margin:8px 0 0}
    h2{font-size:18px;margin:0 0 12px}
    .item-title{font-weight:600;font-size:16px;margin:0 0 6px}
    .why{margin:8px 0 0;font-size:14px}
    .meta{color:#666;font-size:12px;margin:0 0 6px}
    a{color:#0b5fff;text-decoration:none}
    .footer{color:#666;font-size:12px;text-align:center;margin-top:24px}
    .badge{display:inline-block;background:#eef3ff;color:#2141ff;border-radius:999px;padding:2px 8px;font-size:11px;margin-right:6px}
    .section{margin-top:24px}
    .li{margin:12px 0}
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h1>Daily ERP & Middleware Brief</h1>
      <p class="meta">{{ date_str }}</p>
      <p>Here are {{ items|length }} things worth your time today.</p>
    </div>

    {% if top %}
    <div class="card section">
      <h2>Top 5</h2>
      {% for it in top %}
      <div class="li">
        <div class="item-title"><a href="{{ it.url }}">{{ it.title }}</a></div>
        <div class="meta">{{ it.source }} · {{ it.published_human }}</div>
        <div>{{ it.summary }}</div>
        {% if it.why %}<div class="why">• <strong>Why it matters:</strong> {{ it.why }}</div>{% endif %}
      </div>
      {% endfor %}
    </div>
    {% endif %}

    {% if rest %}
    <div class="card section">
      <h2>Also noteworthy</h2>
      {% for it in rest %}
      <div class="li">
        <div class="item-title"><a href="{{ it.url }}">{{ it.title }}</a></div>
        <div class="meta">{{ it.source }} · {{ it.published_human }}</div>
        <div>{{ it.summary }}</div>
        {% if it.why %}<div class="why">• <strong>Why it matters:</strong> {{ it.why }}</div>{% endif %}
      </div>
      {% endfor %}
    </div>
    {% endif %}

    <div class="footer">
      You’re receiving this because you subscribed to the Daily ERP & Middleware Brief.
      <br/>
      <a href="{{ unsubscribe_url }}">Unsubscribe</a>
      • {{ sender_name }} • {{ sender_email }}
    </div>
  </div>
</body>
</html>
